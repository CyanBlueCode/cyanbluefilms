import { Typography } from '@mui/material';

const Gallery = () => (
  <div style={{ paddingTop: '5rem', textAlign: 'center' }}>
    <Typography variant='h3'>Gallery Placeholder</Typography>
    <Typography variant='body1'>Gallery implementation coming soon</Typography>
  </div>
);

export default Gallery;
